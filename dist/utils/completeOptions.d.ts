import { CollectorOptions } from '../types';
export declare const DEFAULT_SESSION_REJECTION: () => boolean;
export default function completeOptions(options?: Partial<CollectorOptions>): CollectorOptions;
