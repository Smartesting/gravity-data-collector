{
  "name": "gravity-data-collector",
  "version": "1.0.0",
  "description": "The browser lib to collect your users behaviors and push the data in your Gravity project",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags",
    "build-sandbox": "browserify lib/index.js -p esmify -o dist/bundle.js",
    "build-minified": "browserify lib/index.js -p esmify -p tinyify -o dist/gravity-logger-min.js",
    "watch-sandbox": "watchify lib/index.js -o dist/bundle.js -v"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Smartesting/gravity-data-collector.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Smartesting/gravity-data-collector/issues"
  },
  "homepage": "https://github.com/Smartesting/gravity-data-collector#readme",
  "devDependencies": {
    "@babel/core": "^7.17.12",
    "@babel/preset-env": "^7.17.12",
    "@types/jsdom": "^16.2.14",
    "@types/node": "^18.0.0",
    "@types/uuid": "^8.3.4",
    "babelify": "^10.0.0",
    "browserify": "^17.0.0",
    "core-js": "^3.22.5",
    "esmify": "^2.1.1",
    "jsdom": "^19.0.0",
    "prettier": "^2.6.1",
    "regenerator-runtime": "^0.13.9",
    "tinyify": "^3.0.0",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.6.3",
    "vitest": "^0.12.4",
    "watchify": "^4.0.0"
  },
  "dependencies": {
    "@cypress/unique-selector": "^0.4.4",
    "uuid": "^8.3.2"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "@babel/preset-env"
          ]
        }
      ]
    ]
  }
}
